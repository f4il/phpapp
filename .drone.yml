publish:
  docker:
    repo: f4il/phpapp
    tags: droneci
    storage_driver: overlay
    username: f4il
    password: xxxx
#  commands:
#    -  docker ps
#    - docker build -t phpapp .
#    - docker run  -d -p 8686:80 --name phpapp phpapp
#    - docker exec -it phpapp curl 127.0.0.1:80
